# Pre-existing channel credentials on a foreign profile. The value is a neutral
# placeholder (no real token shape, no snowflake ids) so the sanitization guard
# stays clean; assimilate only checks the KEY is present and non-empty.
DISCORD_BOT_TOKEN=placeholder-discord-token
