<!-- USER.md — durable facts about the operator (preferences, workflow, constraints).
     One entry per block; separate entries with a line containing only "§".
     Memory convention is documented once in the template README ("Memory convention"). -->
